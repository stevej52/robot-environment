[Login]
# Robot runs headless: the GDM greeter otherwise grabs the power key to show a
# dialog nobody can see. Act on the case button regardless of session inhibitors.
HandlePowerKey=poweroff
PowerKeyIgnoreInhibited=yes
